
   #------Prepare to start Routine "trial"-------
    t = 0
    trialClock.reset()  # clock 
    frameN = -1
    routineTimer.add(60.000000)
    # update component parameters for each repeat
    sound_1.setSound(Anweisung)
->  Trigger = Trigger2
    # keep track of which components have finished
    trialComponents = []
    trialComponents.append(sound_1)
    trialComponents.append(text)
    trialComponents.append(p_port)
    for thisComponent in trialComponents:
        if hasattr(thisComponent, 'status'):
            thisComponent.status = NOT_STARTED


Trigger = Trigger2: Weil Trigger2 im Excelfile die Trigger hat.

Ist im "Codeschnipsel" im Builder schon drin, muss nicht mehr gemacht werden

Was man noch machen muss: Instruktionen Ruhe EEG...

Conditions:
1: OCCO - COOC
2: COOC - OCCO
